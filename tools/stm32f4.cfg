# STM32F4 OpenOCD Configuration
# For use with ST-Link debugger

# Source the interface configuration
source [find interface/stlink.cfg]

# Source the target configuration
source [find target/stm32f4x.cfg]

# Set adapter speed
adapter speed 1000

# Note: arm semihosting enable is handled in the flash script after init
